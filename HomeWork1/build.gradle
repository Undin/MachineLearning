apply plugin: 'java'

repositories {
    mavenCentral()
}

dependencies {
    compile project(':TestModule')
    compile project(':AlgorithmLibrary')
    compile fileTree(dir: 'libs', include: '*.jar')
    testCompile group: 'junit', name: 'junit', version: '4.11'
}
